# MiniPay Reconnect Dashboard Check

Reconnect MiniPay after dashboard data loads and confirm the same wallet context is restored.
Record the wallet prefix, chain label, and reconnect result.
Do not store full wallet addresses unless support requests them.
