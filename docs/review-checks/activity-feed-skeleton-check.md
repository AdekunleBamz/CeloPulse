# Activity Feed Skeleton Check

- Load the activity feed on a slow connection.
- Confirm skeleton rows reserve the same space as loaded events.
- Verify score and timestamp columns do not shift when data arrives.
